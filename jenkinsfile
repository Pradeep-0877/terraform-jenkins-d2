pipeline {
    agent any
    stages {
        stage("init"){
            steps{
                sh "terraform init"
            }
        }
        stage("apply"){
            steps{
                as "terraform apply --auto-approve"
            }

        }
    }
}
