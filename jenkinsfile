pipeline {
    agent any
    stages {
        stage("init"){
            steps{
                sh "terraform init"
                sh "ls -la"
            }
        }
        stage("apply"){
            steps{
                sh "ls -la"
                sh "terraform apply --auto-approve"
            }

        }
    }
}
