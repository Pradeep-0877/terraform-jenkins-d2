pipeline {
    agent any
    stages {
        stage("init"){
            steps{
                sh "terraform init"
                sh "la -la"
            }
        }
        stage("apply"){
            steps{
                sh "ls -la"
                sh "terraform apply --auto-approve"
            }

        }
    }
}
