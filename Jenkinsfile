// Lets start with Jenkins pipeline
// Declerative 
pipeline {
    agent any 
    stages {
        stage ('init') {
            steps {
                sh "terraform init"
            }
        }
    }
}